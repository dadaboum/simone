<div class="container">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <% @forms.each do |form| %>
        <p>Hospital id: <%= form.hospital_id %> - Typeform id: <%= form.typeform_id %> - <a target="_blank" href="https://davidbenamran.typeform.com/to/<%=form.typeform_id %>">Typeform link</a>
      <% end %>
    </div>
  </div>
  <div class="text-center">
    <%= link_to "Add new form", new_form_path, class: "btn btn-primary" %>
  </div>
</div>
