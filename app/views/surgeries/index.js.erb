document.querySelector(".content-container").innerHTML = '<%= j render "index_content" %>';
// pour voir les urls: window.history.pushState({urlPath: '/surgeries?surgery_id=' + '<%= params[:surgery_id] %>' }, '', '/surgeries?surgery_id=' + '<%= params[:surgery_id] %>');

document.querySelector("#event_description").innerText = ("");

var head = document.querySelector("head");
var scriptElement = document.createElement("script");
scriptElement.src = '<%= javascript_pack_tag "application" %>'.match(/src="(.*)">/)[1];
head.appendChild(scriptElement);


